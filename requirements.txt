# === Dependencias del TFB — Impacto económico del estrés hídrico ===
# Floors conservadores. Tras instalar, fija versiones exactas con:
#     pip freeze > requirements.lock.txt
# (la reproducibilidad exacta es un criterio que valora el tribunal)

# --- Ingesta de datos (API ODS de la ONU) ---
requests>=2.31

# --- Manipulación de datos ---
pandas>=2.0
numpy>=1.24

# --- Almacenamiento (SQLite via SQLAlchemy Engine, D-11/D-12) ---
sqlalchemy>=2.0          # Engine para pandas to_sql/read_sql (src/db.py)

# --- Notebooks / análisis exploratorio (3.3) ---
jupyter>=1.0
ipykernel>=6.25
matplotlib>=3.7
seaborn>=0.12

# --- Modelos de panel con efectos fijos (3.4, 3.5) ---
statsmodels>=0.14
linearmodels>=5.3        # PanelOLS: efectos fijos por país (Baltagi, Wooldridge)

# --- Machine learning (3.4, 3.5, 4.3) ---
scikit-learn>=1.3

# --- Interpretabilidad (4.4) ---
shap>=0.43               # importancia de variables (Molnar)

# --- Dashboard geoespacial (4.5) ---
plotly>=5.18             # choropleth sin dependencias de GDAL (más estable en Windows)
streamlit>=1.30          # alternativa: dash
# geopandas / folium: opcionales; en Windows requieren GDAL, más frágil de instalar
