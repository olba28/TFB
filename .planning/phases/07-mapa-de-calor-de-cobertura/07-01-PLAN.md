---
phase: 07-mapa-de-calor-de-cobertura
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/coverage.py
  - tests/test_coverage.py
autonomous: true
requirements: [COVER-01]
must_haves:
  truths:
    - "build_presence_matrix returns a country x year boolean grid where True means a non-null value exists for (country, year, indicator)"
    - "A wholly-absent (country, year) row and a present row with value IS NULL both resolve to the same False (missing) state per D-04"
    - "ordered_countries_with_boundaries returns countries grouped by SDG region plus the row indices where each region group starts"
    - "The coverage signal is computed from raw_observations + country_reference only, never from panel_clean (COVER-01, Pitfall 3)"
  artifacts:
    - "src/coverage.py"
    - "tests/test_coverage.py"
  key_links:
    - "src/coverage.py::build_presence_matrix consumes raw_observations rows read via src.db.get_engine"
    - "src/coverage.py::ordered_countries_with_boundaries joins country_reference on country_code (how=left), mirroring src/panel_build.py::build_clean_panel"
---

<objective>
Create `src/coverage.py`, a pure, unit-tested transform module that computes the raw-data coverage/presence signal for the Phase 7 heatmap, plus `tests/test_coverage.py` covering the five behaviors named in 07-VALIDATION.md's Per-Task Verification Map.

Purpose: The heatmap's correctness (COVER-01) lives entirely in this module's D-04 "missing = row-absent OR value IS NULL" logic and D-03 region-grouping logic. Extracting it into a tested `src/*.py` module — rather than notebook-inline code — matches every prior analytical phase (2-6) and makes the coverage computation verifiable without rendering a figure. This resolves RESEARCH.md Open Question 1 (new module, not notebook-inline) explicitly, per the panel_build.py precedent confirmed in 07-PATTERNS.md.
Output: `src/coverage.py` (two pure functions + INDICATOR_CODES/YEARS constants), `tests/test_coverage.py` (five passing unit tests).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-mapa-de-calor-de-cobertura/07-CONTEXT.md
@.planning/phases/07-mapa-de-calor-de-cobertura/07-RESEARCH.md
@.planning/phases/07-mapa-de-calor-de-cobertura/07-PATTERNS.md
@.planning/phases/07-mapa-de-calor-de-cobertura/07-VALIDATION.md
@src/panel_build.py
@src/db.py
@tests/test_panel_build.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/coverage.py with build_presence_matrix and ordered_countries_with_boundaries</name>
  <files>src/coverage.py</files>
  <read_first>
    - src/panel_build.py (exact analog: module docstring style, INDICATOR_CODES constant, the pivot/reindex idiom in compute_coverage lines 46-70, the pivot + reset_index + left-merge-on-country_code shape in build_clean_panel lines 84-93, REFERENCE_COLUMNS)
    - src/db.py (module docstring's fixed-literal-SQL-table-name Security V5 convention to mirror; get_engine signature — read-only reference, coverage.py itself takes DataFrames as args, not an engine)
    - .planning/phases/07-mapa-de-calor-de-cobertura/07-RESEARCH.md ("Code Examples" section — vetted build_presence_matrix and ordered_countries_with_boundaries reference implementations; "Anti-Patterns to Avoid")
    - .planning/phases/07-mapa-de-calor-de-cobertura/07-PATTERNS.md (Pattern Assignments for src/coverage.py)
  </read_first>
  <behavior>
    Encodes the expectations that tests/test_coverage.py (Task 2) will assert:
    - build_presence_matrix(raw_observations, countries, indicator_code, years=None): for a (country, year) with a non-null value for that indicator -> cell is True. For a (country, year) whose row is entirely absent -> cell is False. For a (country, year) whose row exists but value IS NULL -> cell is False (same state as absent, D-04). Returned frame is indexed by `countries` (in the given order) with columns = `years` (default YEARS = 2000..2022).
    - ordered_countries_with_boundaries(country_reference, countries): returns (ordered_country_list, boundary_indices). ordered_country_list contains exactly the members of `countries` present in country_reference, sorted by (region, subregion, country_code). boundary_indices are the 0-based row positions where a new region group begins (index 0 is always the first boundary).
    - Neither function reads, imports, or references panel_clean or panel_exclusions in any way (COVER-01, Pitfall 3).
  </behavior>
  <action>
    Create `src/coverage.py` following `src/panel_build.py`'s exact module shape. Open with a Google-style module docstring stating what the module computes and WHY the D-04 unified-missing-state logic works (row-absent and stored-NULL both deserialize to NaN through pivot+reindex, so a single .notna() unifies them with no per-cell branching), plus a one-line note that all callers read fixed-literal table names only (Security V5, mirroring src/db.py). Then `from __future__ import annotations`, `import pandas as pd`. Define module constants `INDICATOR_CODES: list[str] = ["6.4.2", "6.4.1", "8.1.1", "8.2.1", "2.3.1"]` (copied literal, not imported from panel_build — standalone-module convention) and `YEARS: list[int] = list(range(2000, 2023))`.

    Implement `build_presence_matrix(raw_observations: pd.DataFrame, countries: list[str], indicator_code: str, years: list[int] | None = None) -> pd.DataFrame`: default years to YEARS; filter raw_observations to rows where indicator_code matches; pivot with index="country_code", columns="year", values="value"; reindex(index=countries, columns=years) so absent (country, year) combinations become NaN; return `.notna()`. Do NOT write any per-cell existence/nullity loop — the pivot+reindex+notna chain is the whole implementation (Don't Hand-Roll table, RESEARCH.md).

    Implement `ordered_countries_with_boundaries(country_reference: pd.DataFrame, countries: list[str]) -> tuple[list[str], list[int]]`: restrict country_reference to rows whose country_code is in `countries`; sort_values(["region", "subregion", "country_code"], na_position="last"); collect the ordered country_code list; walk the sorted region column tracking a sentinel `prev_region` and append the row index each time the region changes (yielding [0, ...] boundary positions). Return (ordered, boundaries). Join/selection must use country_reference columns ["country_code", "region", "subregion"] only, on country_code, mirroring build_clean_panel's how="left" merge shape — never touch panel_clean.

    Do NOT add a `_main()` / `if __name__ == "__main__"` CLI entry (07-PATTERNS.md: coverage.py is read-only visualization support; D-05 fixes the notebook as the sole entry point). Type-hint every signature; keep functions under 50 lines each (project Function Design convention). Ruff-format on save.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -c "from src import coverage; assert hasattr(coverage, 'build_presence_matrix') and hasattr(coverage, 'ordered_countries_with_boundaries') and coverage.INDICATOR_CODES == ['6.4.2','6.4.1','8.1.1','8.2.1','2.3.1'] and coverage.YEARS == list(range(2000,2023))"</automated>
  </verify>
  <acceptance_criteria>
    - `src/coverage.py` exists and imports cleanly via `.venv/Scripts/python.exe -c "from src import coverage"` with no error.
    - Module exposes `build_presence_matrix`, `ordered_countries_with_boundaries`, `INDICATOR_CODES` (exactly `["6.4.2","6.4.1","8.1.1","8.2.1","2.3.1"]`), and `YEARS` (exactly `list(range(2000, 2023))`).
    - `grep -n "panel_clean\|panel_exclusions" src/coverage.py` returns no matches (COVER-01 / Pitfall 3 guard).
    - `grep -n "_main\|__main__" src/coverage.py` returns no matches (no CLI entry, per D-05).
    - `.venv/Scripts/python.exe -m ruff check src/coverage.py` reports no errors.
  </acceptance_criteria>
  <done>src/coverage.py exists with both pure functions, the two module constants, no panel_clean reference, no CLI entry, and passes Ruff.</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/test_coverage.py with the five COVER-01 unit tests</name>
  <files>tests/test_coverage.py</files>
  <read_first>
    - tests/test_panel_build.py (exact analog: the tmp_path-backed `engine` fixture lines 20-25, the `_obs_row`/`_seed_raw_observations`/`_seed_country_reference` seed helpers lines 28-76, the "seed -> read fresh from engine -> call pure function -> filter -> assert scalar" assertion style)
    - src/coverage.py (the module under test, created in Task 1)
    - src/db.py (get_engine, init_db, insert_observations signatures for the fixture)
    - .planning/phases/07-mapa-de-calor-de-cobertura/07-VALIDATION.md (Per-Task Verification Map — the five exact test names that MUST exist)
    - .planning/phases/07-mapa-de-calor-de-cobertura/07-PATTERNS.md (tests/test_coverage.py Pattern Assignments — including the panel_clean-never-read test approach: simply do not seed panel_clean in that test's fixture and assert the call still succeeds)
  </read_first>
  <action>
    Create `tests/test_coverage.py` mirroring `tests/test_panel_build.py`'s structure. Open with a short module docstring, `from __future__ import annotations`, `import pandas as pd`, `import pytest`, `from src import db, coverage`. Reuse verbatim the `engine(tmp_path)` fixture (get_engine on tmp_path / "test_panel.db", then init_db) — never touch data/panel.db. Reuse the `_obs_row(country, indicator, year, value, ...)` helper shape; write a small `_seed` helper (or inline seeding) that inserts a synthetic raw_observations set via db.insert_observations, and a `_seed_country_reference(engine)` helper writing a synthetic country_reference via `to_sql("country_reference", engine, if_exists="replace", index=False)` covering at least two distinct regions.

    Implement exactly these five test functions (names are contract — 07-VALIDATION.md references them):
    - `test_build_presence_matrix_absent_row_is_missing`: seed a country/indicator with a gap year (no row at all for one year); read raw fresh from engine; assert build_presence_matrix(...).loc[country, gap_year] is False (numpy/py bool).
    - `test_build_presence_matrix_null_value_is_missing`: seed a row for (country, year, indicator) with value=None via the _obs_row helper (a NULL-value row is a distinct valid key, not a duplicate); assert the corresponding cell is False — same state as the absent-row case (D-04).
    - `test_build_presence_matrix_non_null_value_is_present`: seed a row with a real numeric value; assert the cell is True.
    - `test_ordered_countries_with_boundaries_groups_by_region`: seed a country_reference spanning >=2 regions with members deliberately interleaved by country_code; call ordered_countries_with_boundaries(country_reference, countries); assert the returned ordered list is grouped by region (all members of one region contiguous) and boundary_indices marks each region start (first element 0, and one boundary per additional region at the correct position).
    - `test_coverage_module_never_reads_panel_clean`: build the engine WITHOUT any panel_clean table present, read only raw_observations + country_reference into DataFrames, call build_presence_matrix and ordered_countries_with_boundaries, and assert both return successfully (no sqlite OperationalError, no attempt to read panel_clean) — proving the functions operate purely on the DataFrames passed in.

    Use small explicit years (e.g. a 2000..2004 window) in seeds so the default YEARS=2000..2022 reindex is exercised (absent years beyond the seeded window should surface as False, confirming the full-grid reindex). Pass years explicitly where a tighter assertion is clearer.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_coverage.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_coverage.py -x` passes with all five named tests present and green: `test_build_presence_matrix_absent_row_is_missing`, `test_build_presence_matrix_null_value_is_missing`, `test_build_presence_matrix_non_null_value_is_present`, `test_ordered_countries_with_boundaries_groups_by_region`, `test_coverage_module_never_reads_panel_clean`.
    - `.venv/Scripts/python.exe -m pytest tests/test_coverage.py --collect-only -q` lists exactly those five test node IDs.
    - Full suite `.venv/Scripts/python.exe -m pytest` stays green (no regression in prior phases' tests).
    - The fixture uses a tmp_path SQLite file; `grep -n "data/panel.db" tests/test_coverage.py` returns no matches.
  </acceptance_criteria>
  <done>tests/test_coverage.py exists with the five contract-named tests, all passing, full suite green, no reference to data/panel.db.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite file -> pandas | Local `data/panel.db` (in the notebook; tests use tmp_path) read via `pd.read_sql` — no untrusted/external input crosses this boundary; single-user local analysis |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | SQL reads in src/coverage.py callers | low | accept | No SQL text is built in coverage.py at all — it operates on DataFrames passed as args; callers use fixed-literal table names only, mirroring src/db.py's established parameterized/fixed-literal discipline. No external input reaches SQL. |
| T-07-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new packages introduced (RESEARCH.md Package Legitimacy Audit: N/A — pandas/sqlalchemy already pinned in requirements.lock.txt). No install task exists in this plan. |

**Overall assessment:** Negligible security surface — a local, read-only, no-network pure-transform module with no untrusted input at any boundary (RESEARCH.md Security Domain). ASVS Level 1 controls satisfied by inheriting src/db.py's existing discipline; no new controls needed, no high-severity threats.
</threat_model>

<verification>
- `.venv/Scripts/python.exe -m pytest tests/test_coverage.py -x` — all five COVER-01 tests green.
- `.venv/Scripts/python.exe -m pytest` — full suite green (no regression).
- `.venv/Scripts/python.exe -m ruff check src/coverage.py tests/test_coverage.py` — clean.
- `grep -n "panel_clean\|panel_exclusions" src/coverage.py` — no matches (raw-data-only coverage source, COVER-01).
</verification>

<success_criteria>
- src/coverage.py exposes build_presence_matrix and ordered_countries_with_boundaries as pure, tested functions.
- D-04 unified missing-state (absent row == NULL value == missing) is proven by two distinct passing tests.
- D-03 region grouping + boundary indices proven by a passing test.
- panel_clean is never read for the coverage signal (proven by test + grep).
- COVER-01's five verification-map tests all exist and pass.
</success_criteria>

<artifacts_this_phase_produces>
This plan (07-01) creates:
- `src/coverage.py::build_presence_matrix` — pure function, country x year presence/absence grid per indicator (D-04)
- `src/coverage.py::ordered_countries_with_boundaries` — pure function, region-grouped country order + boundary indices (D-03)
- `src/coverage.py::INDICATOR_CODES` — `["6.4.2", "6.4.1", "8.1.1", "8.2.1", "2.3.1"]`
- `src/coverage.py::YEARS` — `list(range(2000, 2023))`
- `tests/test_coverage.py` — five COVER-01 unit tests

Consumed downstream by Plan 07-02's notebook (`notebook/7_1_mapa_calor_cobertura.ipynb`), which produces `figuras/07_mapa_calor_cobertura.png`.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/07-mapa-de-calor-de-cobertura/07-01-SUMMARY.md` when done.
</output>
