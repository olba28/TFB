---
phase: 07-mapa-de-calor-de-cobertura
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - notebook/7_1_mapa_calor_cobertura.ipynb
  - figuras/07_mapa_calor_cobertura.png
autonomous: false
requirements: [COVER-02]
must_haves:
  truths:
    - "Running the notebook end-to-end (Kernel -> Restart & Run All) produces exactly one PNG at figuras/07_mapa_calor_cobertura.png"
    - "The figure is a grid of 5 sub-heatmaps (one per ODS indicator), each country x year, combined into a single figure/PNG (D-01, D-02)"
    - "Each of the 5 ODS indicator codes (6.4.2, 6.4.1, 8.1.1, 8.2.1, 2.3.1) is identifiable in the figure (subplot titles)"
    - "Country rows are grouped by SDG region with visual separators between groups (D-03)"
    - "Coverage is computed from raw_observations + country_reference only; src/dashboard/ is not modified (COVER-02)"
  artifacts:
    - "notebook/7_1_mapa_calor_cobertura.ipynb"
    - "figuras/07_mapa_calor_cobertura.png"
  key_links:
    - "notebook imports src.coverage.build_presence_matrix + ordered_countries_with_boundaries (from Plan 07-01)"
    - "notebook reads data/panel.db via src.db.get_engine, tables raw_observations + country_reference (never panel_clean)"
    - "plt.savefig writes the single PNG to figuras/07_mapa_calor_cobertura.png"
---

<objective>
Create `notebook/7_1_mapa_calor_cobertura.ipynb`, a thin orchestration/visualization notebook that loads `raw_observations` + `country_reference` from `data/panel.db`, calls `src.coverage` (Plan 07-01) for each of the 5 ODS indicators, renders a single combined figure of 5 country x year sub-heatmaps grouped by SDG region, and saves it to `figuras/07_mapa_calor_cobertura.png` for the thesis annex.

Purpose: This is the deliverable of the phase (COVER-02) — the static PNG for the memoria annex, regenerable reproducibly from a re-executable notebook (Success Criteria #4), with the coverage signal computed on raw data before the Phase 2 70% filter (Success Criteria #2). D-05 fixes the notebook as the sole entry point.
Output: `notebook/7_1_mapa_calor_cobertura.ipynb` (executed, outputs cleared or committed per repo convention) and `figuras/07_mapa_calor_cobertura.png`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-mapa-de-calor-de-cobertura/07-CONTEXT.md
@.planning/phases/07-mapa-de-calor-de-cobertura/07-RESEARCH.md
@.planning/phases/07-mapa-de-calor-de-cobertura/07-PATTERNS.md
@.planning/phases/07-mapa-de-calor-de-cobertura/07-VALIDATION.md
@src/coverage.py
@src/db.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create and execute notebook/7_1_mapa_calor_cobertura.ipynb producing the single PNG</name>
  <files>notebook/7_1_mapa_calor_cobertura.ipynb, figuras/07_mapa_calor_cobertura.png</files>
  <read_first>
    - src/coverage.py (the functions to call: build_presence_matrix, ordered_countries_with_boundaries, INDICATOR_CODES, YEARS — created in Plan 07-01)
    - notebook/2_1_construccion_panel_eda.ipynb (style precedent ONLY: the PROJECT_ROOT/sys.path bootstrap cell, and the sns.heatmap style at cell 9 — but that cell reads panel_clean, which this notebook MUST NOT do; read it to copy the bootstrap + heatmap styling, not the data source)
    - .planning/phases/07-mapa-de-calor-de-cobertura/07-RESEARCH.md ("Code Examples" -> notebook orchestration sketch; "Common Pitfalls" -> real 8-region sizes 4-49 and the 215-country label-density tradeoff; "Architecture Patterns" -> binary ListedColormap + Patch legend + axhline separators)
    - .planning/phases/07-mapa-de-calor-de-cobertura/07-PATTERNS.md (notebook Pattern Assignments: bootstrap cell verbatim, Reds-vs-binary note)
  </read_first>
  <action>
    Create `notebook/7_1_mapa_calor_cobertura.ipynb` as a thin orchestrator (all analytical logic lives in src/coverage.py from Plan 07-01; the notebook only loads, calls, plots, saves).

    Cell 1 (markdown): title + one-line purpose (raw-data coverage heatmap for the memoria annex; computed on raw_observations BEFORE the Phase 2 70% filter, per COVER-01 / Success Criteria #2).
    Cell 2 (code): the PROJECT_ROOT/sys.path bootstrap copied verbatim from notebook 2_1 (`PROJECT_ROOT = Path.cwd().parent if Path.cwd().name == "notebook" else Path.cwd()`, insert into sys.path), then `import matplotlib.pyplot as plt`, `import pandas as pd`, `import seaborn as sns`, `from matplotlib.colors import ListedColormap`, `from matplotlib.patches import Patch`, `from src import db, coverage`.
    Cell 3 (code): `engine = db.get_engine(str(PROJECT_ROOT / "data" / "panel.db"))`; `raw_observations = pd.read_sql("SELECT * FROM raw_observations", engine)`; `country_reference = pd.read_sql("SELECT * FROM country_reference", engine)`. Use fixed-literal table names only (Security V5). Do NOT read panel_clean or panel_exclusions anywhere.
    Cell 4 (code): `countries = sorted(raw_observations["country_code"].unique())`; `ordered_countries, boundaries = coverage.ordered_countries_with_boundaries(country_reference, countries)`.
    Cell 5 (code): build the figure. Create a 1x5 subplot grid (`plt.subplots(1, 5, figsize=..., sharey=True)`) — one sub-heatmap per indicator in `coverage.INDICATOR_CODES` (D-01). Size the figure height around the REAL region distribution (8 SDG regions, sizes 4 to 49, total 215 countries — NOT the ~40-45/5-7 CONTEXT.md pre-verification estimate; note this deviation in a short markdown/comment for the record). Define `cmap = ListedColormap(["#e8e8e8", "#2b6cb0"])` (light gray = missing, blue = present) — a binary status palette, NOT a sequential `cmap="Reds"` ramp (RESEARCH.md Anti-Patterns: binary state, not magnitude). For each (ax, indicator): `matrix = coverage.build_presence_matrix(raw_observations, ordered_countries, indicator)`; `sns.heatmap(matrix.astype(int), cmap=cmap, cbar=False, ax=ax, yticklabels=False)`; draw `ax.axhline(y=b, color="black", linewidth=0.8)` for each b in `boundaries` (D-03 region separators); `ax.set_title(indicator)`; `ax.set_xlabel("Año")`. On the leftmost subplot only, render per-country y-tick labels at a small font (~3.5-4pt) at the row centers so all 215 countries are identifiable when the PNG is zoomed digitally — targeting 300 DPI digital/PDF-zoom legibility, NOT physical print-page per-country legibility (resolves RESEARCH.md Open Question 2 / Assumption A3; note this choice explicitly in a comment). Add ONE shared figure legend via `fig.legend(handles=[Patch(color="#e8e8e8", label="Sin dato"), Patch(color="#2b6cb0", label="Dato presente")], loc="lower center", ncol=2)` and a `fig.suptitle(...)` naming the figure (cobertura de datos crudos por indicador ODS, país y año, 2000-2022).
    Cell 6 (code): `plt.savefig(PROJECT_ROOT / "figuras" / "07_mapa_calor_cobertura.png", dpi=300, bbox_inches="tight")` then `plt.show()`. Exactly ONE savefig call -> ONE PNG (D-02, COVER-02).

    Do NOT modify anything under `src/dashboard/`, `src/db.py`, `src/panel_build.py`, or any Phase 1-6 module (COVER-02: no dashboard changes; this phase is read-only + one new figure). After authoring, execute the notebook end-to-end via nbconvert to confirm it runs clean and regenerates the PNG.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m jupyter nbconvert --to notebook --execute --output-dir "$TEMP" notebook/7_1_mapa_calor_cobertura.ipynb && test -f figuras/07_mapa_calor_cobertura.png && echo PNG_OK</automated>
  </verify>
  <acceptance_criteria>
    - `jupyter nbconvert --to notebook --execute notebook/7_1_mapa_calor_cobertura.ipynb` completes with no cell error (reproducible end-to-end, Success Criteria #4).
    - After execution, `figuras/07_mapa_calor_cobertura.png` exists (exactly one PNG produced — COVER-02, D-02).
    - `grep -c "panel_clean\|panel_exclusions" notebook/7_1_mapa_calor_cobertura.ipynb` returns 0 (raw-data-only coverage source; note: search the ipynb JSON source — the tokens must not appear in any code cell).
    - `git status --porcelain src/dashboard/ src/db.py src/panel_build.py` shows no modifications (COVER-02: dashboard/pipeline untouched).
    - The notebook calls `coverage.build_presence_matrix` five times (once per code in `coverage.INDICATOR_CODES`) and `coverage.ordered_countries_with_boundaries` once — verifiable by inspecting the ipynb source.
    - Exactly one `plt.savefig(` call targeting `figuras/07_mapa_calor_cobertura.png` at `dpi=300`.
  </acceptance_criteria>
  <done>The notebook exists, runs end-to-end via nbconvert with no error, produces exactly one PNG at figuras/07_mapa_calor_cobertura.png, references src.coverage (never panel_clean), and touches nothing under src/dashboard/.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify the coverage-heatmap PNG against the 4 Roadmap Success Criteria</name>
  <action>Present the generated figuras/07_mapa_calor_cobertura.png to the developer and walk through the verification steps below; block until the developer approves or requests changes.</action>
  <what-built>
    A single static PNG at `figuras/07_mapa_calor_cobertura.png` — a grid of 5 country x year sub-heatmaps (one per ODS indicator: 6.4.2, 6.4.1, 8.1.1, 8.2.1, 2.3.1), computed on raw_observations (before the Phase 2 70% filter), with countries grouped by SDG region (separator lines between groups) and a binary "Sin dato / Dato presente" legend. Generated reproducibly by notebook/7_1_mapa_calor_cobertura.ipynb.
  </what-built>
  <how-to-verify>
    1. Open `figuras/07_mapa_calor_cobertura.png` (zoom in on a PDF viewer / image viewer as needed).
    2. Confirm the figure shows FIVE distinct sub-heatmaps, each labeled with one of the 5 ODS indicator codes (6.4.2, 6.4.1, 8.1.1, 8.2.1, 2.3.1) — Success Criteria #3.
    3. Confirm each sub-heatmap has a country (y) x year (x) layout, with years spanning 2000-2022 — Success Criteria #1.
    4. Confirm country rows are grouped into region bands with visible separator lines between groups (D-03), and that the leftmost subplot's small country labels are legible when zoomed at 300 DPI.
    5. Confirm the legend distinguishes "Sin dato" (missing) from "Dato presente" — a binary present/absent encoding, not a magnitude gradient.
    6. Sanity-check Success Criteria #2 (raw, pre-filter coverage): the figure should show data presence for country/indicator pairs that panel_clean excludes — e.g. the 2.3.1 (agricultural) panel should show scattered presence for many countries, including sparse/low-coverage ones that the 70% filter would drop. If in doubt, run the notebook cell that reports total missing-cell count and confirm it reflects raw_observations (~7,891 missing among the 215x23x5 grid), not panel_exclusions' filtered count.
    7. Optionally run `Kernel -> Restart & Run All` in Jupyter to confirm the PNG regenerates identically from a clean kernel (Success Criteria #4).
  </how-to-verify>
  <resume-signal>Type "approved" if the figure satisfies all 4 Roadmap Success Criteria, or describe what needs to change (DPI, layout, labels, palette, region ordering).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite file -> pandas | Local `data/panel.db` read via `pd.read_sql` with fixed-literal table names — no untrusted/external input crosses this boundary |
| Notebook -> filesystem (savefig) | PNG written to a fixed-literal path `figuras/07_mapa_calor_cobertura.png` — path is never derived from external/untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-02 | Tampering | pd.read_sql calls in the notebook | low | accept | Fixed-literal table names ("raw_observations", "country_reference") only; no variable interpolation into SQL text, mirroring src/db.py's discipline. No external input reaches SQL. |
| T-07-03 | Tampering | plt.savefig output path | low | accept | Output path is a fixed literal (`figuras/07_mapa_calor_cobertura.png`), never derived from user/external input — no path-traversal surface. |
| T-07-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new packages (matplotlib/seaborn/pandas already pinned in requirements.lock.txt; RESEARCH.md Package Legitimacy Audit: N/A). No install task in this plan. |

**Overall assessment:** Negligible security surface — local, read-only, no-network, single-file-output visualization with no untrusted input at any boundary (RESEARCH.md Security Domain). ASVS Level 1 satisfied by inheriting src/db.py's fixed-literal-query discipline; no high-severity threats.
</threat_model>

<verification>
- `jupyter nbconvert --to notebook --execute notebook/7_1_mapa_calor_cobertura.ipynb` runs clean end-to-end (Success Criteria #4).
- `figuras/07_mapa_calor_cobertura.png` exists after execution (COVER-02, D-02).
- No modifications under `src/dashboard/`, `src/db.py`, `src/panel_build.py` (COVER-02).
- No `panel_clean`/`panel_exclusions` reference anywhere in the notebook (COVER-01 raw-data source).
- Human-verify checkpoint: the PNG satisfies all 4 Roadmap Success Criteria (5 labeled indicators, country x year axes, region grouping, missing/present legend, raw pre-filter coverage).
</verification>

<success_criteria>
- One PNG at figuras/07_mapa_calor_cobertura.png showing a 5-indicator grid of country x year coverage sub-heatmaps.
- Coverage computed from raw_observations (verifiably includes pairs panel_clean excludes) — Success Criteria #2.
- The 5 ODS indicator codes are identifiable in the figure — Success Criteria #3.
- The figure regenerates reproducibly from the re-executable notebook, with no dashboard changes — Success Criteria #4.
</success_criteria>

<artifacts_this_phase_produces>
This plan (07-02) creates:
- `notebook/7_1_mapa_calor_cobertura.ipynb` — D-05 entry point; loads raw_observations + country_reference, calls src.coverage, renders the 5-indicator grid, saves the PNG
- `figuras/07_mapa_calor_cobertura.png` — the single static coverage-heatmap PNG for the thesis annex (COVER-02, D-02)

Consumes from Plan 07-01: `src/coverage.py::build_presence_matrix`, `src/coverage.py::ordered_countries_with_boundaries`, `src/coverage.py::INDICATOR_CODES`.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/07-mapa-de-calor-de-cobertura/07-02-SUMMARY.md` when done.
</output>
