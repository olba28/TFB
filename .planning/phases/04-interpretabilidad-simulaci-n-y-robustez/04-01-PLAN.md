---
phase: 04-interpretabilidad-simulaci-n-y-robustez
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/simulate.py
  - tests/test_simulate.py
autonomous: true
requirements: [INTERP-01, INTERP-03, REPRO-02]

must_haves:
  truths:
    - "resample_entities relabels every drawn country to a unique synthetic entity id, so N draws yield exactly N recognized entities (no silent collision)"
    - "bootstrap_counterfactual returns per-scenario coefficient/effect distributions with 2.5/97.5 percentile CIs for at least three reduction scenarios (-10/-20/-30%)"
    - "check_non_extrapolation excludes a country from a scenario (never caps) when its simulated water-stress value falls below the global historical minimum, per scenario"
    - "Two calls to bootstrap_counterfactual with the same seed produce bit-identical coefficient draws (REPRO-02)"
    - "fit_interaction_model returns one water-stress coefficient per group (region, is_ldc) with SE/CI and never raises AbsorbingEffectError"
  artifacts:
    - src/simulate.py
    - tests/test_simulate.py
  key_links:
    - "simulate.py imports panel_base.fit_panel_model unmodified and calls it once per bootstrap replica"
    - "resample_entities output feeds fit_panel_model with a unique (entity, year) index"
    - "reduction applied on each country's last-observed-year (2022) water-stress value (D-03)"
---

<objective>
Build `src/simulate.py`: the parametric, dependent-variable-agnostic panel-econometrics post-hoc analysis module for Phase 4 — block bootstrap counterfactual with per-scenario confidence intervals (INTERP-01), non-extrapolation exclusion (D-04), and interaction-term heterogeneity (INTERP-03). Test-first, mirroring `tests/test_panel_base.py`.

Purpose: Deliver the reproducible sensitivity-simulation and heterogeneity primitives that `notebook/4_1_interpretabilidad_simulacion.ipynb` (Plan 03) orchestrates, designed so Phase 6 can reuse them with `dep_var="2.3.1"` unmodified (D-12).
Output: `src/simulate.py`, `tests/test_simulate.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-interpretabilidad-simulaci-n-y-robustez/04-CONTEXT.md
@.planning/phases/04-interpretabilidad-simulaci-n-y-robustez/04-RESEARCH.md
@.planning/phases/04-interpretabilidad-simulaci-n-y-robustez/04-PATTERNS.md
@.planning/phases/04-interpretabilidad-simulaci-n-y-robustez/04-VALIDATION.md
@src/panel_base.py
@tests/test_panel_base.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing unit tests for src/simulate.py</name>
  <files>tests/test_simulate.py</files>
  <read_first>tests/test_panel_base.py (reuse `_make_synthetic_panel` fixture style, `DEP_VAR="y"`/`INDEP_VARS=["x"]`, `np.random.default_rng` seeding, docstring-per-test convention), 04-RESEARCH.md Pattern 1 + Pattern 2 (live-verified code), 04-VALIDATION.md Per-Task Verification Map + Wave 0 Requirements.</read_first>
  <behavior>
    - test_resample_entities_unique_index: drawing N entities with a fixed rng returns a DataFrame whose `country_code` has exactly N unique relabeled ids (e.g. `f"{entity}__b{i}"`), matching RESEARCH.md's live-verified "N draws -> N recognized entities" finding scaled down (e.g. 10 entities -> 10 unique post-relabel ids even when the same source country is drawn twice).
    - test_non_extrapolation_exclusion: given a synthetic baseline and a historical global minimum, a scenario whose simulated value drops below that minimum for a country lists that country in `excluded_countries` and drops it from that scenario's effect array — never caps/truncates the value (D-04).
    - test_bootstrap_determinism (REPRO-02): two calls to `bootstrap_counterfactual` with `seed=42` on the same synthetic panel return `np.array_equal` coefficient/effect arrays (assert exact equality, NOT pytest.approx).
    - test_interaction_formula (INTERP-03): `fit_interaction_model` on a synthetic panel with a binary group column returns one coefficient per group value with `std_errors`/`conf_int` available and raises no AbsorbingEffectError.
  </behavior>
  <action>Create `tests/test_simulate.py` importing `from src import panel_base, simulate`. Reuse a `_make_synthetic_panel(n_entities, n_years, seed, beta, ...)` helper in the exact style of `tests/test_panel_base.py` (synthetic `(country_code, year)` panel, `np.random.default_rng`, `f"C{i:02d}"` naming). Add a second small helper producing a synthetic panel that also carries a time-invariant `region`/`is_ldc` group column for the interaction test. Write the four tests described in <behavior>. Tests MUST fail at this point because `src/simulate.py` does not yet exist — this is the RED step. Do NOT use the real 171-country `data/panel.db` panel in these unit tests (Wave 0 requirement: fast fixture-based tests only). Docstrings cite the driving decision (D-01/D-04 for bootstrap, D-09/D-10 for interaction, REPRO-02 for determinism).</action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_simulate.py -q 2>&1 | grep -E "error|ModuleNotFoundError|failed" </automated>
  </verify>
  <done>tests/test_simulate.py exists with the four tests and fixtures; collection fails only because `src.simulate` cannot be imported (functions not yet implemented) — confirming the tests genuinely exercise the not-yet-written module (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement bootstrap counterfactual + non-extrapolation in src/simulate.py</name>
  <files>src/simulate.py</files>
  <read_first>src/panel_base.py (module docstring convention, `fit_panel_model` signature, `_build_panel_index`, `choose_cov_type` pure-rule style, `warnings.warn(..., stacklevel=2)` convention), 04-RESEARCH.md Pattern 1 (`resample_entities`, `bootstrap_counterfactual`) + Pitfalls #1/#2/#3, 04-PATTERNS.md `src/simulate.py` section.</read_first>
  <action>Create `src/simulate.py` with `from __future__ import annotations`, a module docstring citing D-01..D-04 and D-12 (Phase 6 reuse with `dep_var="2.3.1"`), and a typed import block mirroring panel_base.py. Implement: (1) `resample_entities(df, entity_col, rng)` — draws entities with replacement via `rng.choice(..., replace=True)` and relabels EACH draw to a unique synthetic id `f"{entity}__b{i}"` before concatenation (mandatory fix for Pitfall #1; without it PanelOLS silently collapses duplicate draws — 171 draws became 109 entities live). (2) `bootstrap_counterfactual(fitted_results, df, dep_var, indep_var, reduction_pcts=[-0.10,-0.20,-0.30], baseline_year=2022, n_replicas=1000, seed=42, entity_col="country_code")` — uses `np.random.SeedSequence(seed).spawn(n_replicas)` for independent reproducible per-replica RNGs (REPRO-02, D-02), refits via `panel_base.fit_panel_model(boot_df, dep_var, [indep_var], entity_effects=True, time_effects=True, cov_type="unadjusted")` per replica, collects `res.params[indep_var]`, computes each scenario's delta on the per-country `baseline_year` (2022) water-stress value (D-03), applies `check_non_extrapolation` (D-04), and returns per-scenario dict with `effect_draws`, `ci_2.5`/`ci_97.5` via `np.percentile(..., [2.5,97.5], axis=0)`, and `excluded_countries`. (3) `check_non_extrapolation(baseline, simulated_level, historical_min)` — pure function returning the surviving mask + excluded-country list; NEVER caps. Emit `warnings.warn(..., UserWarning, stacklevel=2)` if a scenario excludes all countries. Do NOT call `.predict()` on any refit PanelOLS (Pitfall #3 — compute the effect as `coef * delta`, marginal-effect sensitivity, not an absolute level). Do NOT modify src/panel_base.py.</action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_simulate.py::test_resample_entities_unique_index tests/test_simulate.py::test_non_extrapolation_exclusion tests/test_simulate.py::test_bootstrap_determinism -x -q</automated>
  </verify>
  <done>The three bootstrap/non-extrapolation/determinism tests pass; `resample_entities` yields exactly N unique ids for N draws; two same-seed bootstrap runs are bit-identical; excluded countries are listed (not capped).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement interaction-term heterogeneity in src/simulate.py</name>
  <files>src/simulate.py</files>
  <read_first>04-RESEARCH.md Pattern 2 (live-verified `PanelOLS.from_formula` with `:` colon syntax, AbsorbingEffectError anti-pattern), 04-CONTEXT.md D-09/D-10/D-11, src/panel_base.py `_build_panel_index` numeric-coercion helper.</read_first>
  <action>Add `fit_interaction_model(df, dep_var, indep_var, group_col, cov_type="clustered", **cov_config)` to `src/simulate.py`. Build the `(country_code, year)` MultiIndex and coerce numeric columns defensively with `pd.to_numeric(..., errors="coerce")` (reuse the panel_base pattern). Fit with `PanelOLS.from_formula` using the interaction-ONLY formula `Q("{dep_var}") ~ 1 + Q("{indep_var}") : C({group_col}) + EntityEffects + TimeEffects` (colon, NOT `*`) — the standalone `C({group_col})` main effect MUST be omitted because it is time-invariant per entity and fully absorbed by EntityEffects, which otherwise raises AbsorbingEffectError (live-verified, D-09). Default to entity-clustered SEs (`cluster_entity=True`) so `res.params`, `res.std_errors`, `res.conf_int()` give one water-stress coefficient PER group value with SE and CI (D-11 — coefficient table only, never a per-country prediction). Docstring cites D-09/D-10/D-11 and states Phase 6 reuse (`dep_var="2.3.1"`, `group_col` still `region`/`is_ldc`). Keep the function generic in `group_col` so the notebook (Plan 03) calls it twice: once for `region`, once for `is_ldc` (D-10).</action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_simulate.py::test_interaction_formula -x -q</automated>
  </verify>
  <done>fit_interaction_model returns per-group coefficients with SE/CI, raises no AbsorbingEffectError, and never produces a per-country predicted value; full `tests/test_simulate.py` passes.</done>
</task>

</tasks>

## Artifacts this phase produces

**New files:**
- `src/simulate.py` — parametric block-bootstrap/heterogeneity module (D-12), reusable unmodified by Phase 6 with `dep_var="2.3.1"`
- `tests/test_simulate.py` — fixture-based unit tests for the above

**New functions (in `src/simulate.py`):**
- `resample_entities(df, entity_col, rng) -> pd.DataFrame` — draws entities with replacement, relabels each draw to a unique synthetic entity id (fixes Pitfall #1's silent collision bug)
- `bootstrap_counterfactual(fitted_results, df, dep_var, indep_var, reduction_pcts, baseline_year, n_replicas=1000, seed=42, entity_col="country_code") -> dict` — per-scenario coefficient/effect draws, percentile CIs, excluded-country lists
- `check_non_extrapolation(baseline, simulated_level, historical_min)` — pure function; returns surviving mask + excluded-country list (D-04)
- `fit_interaction_model(df, dep_var, indep_var, group_col, cov_type="clustered", **cov_config)` — interaction-term heterogeneity model (INTERP-03/D-09/D-10/D-11)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite → simulate.py | Reads own `panel_clean`/`panel_exclusions` tables (already validated in Phases 1–2); no external/user input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | SQL table/column names in read path | low | accept | Reuse `src/db.py` `get_engine()` + fixed-table `pd.read_sql` pattern unchanged; never construct SQL from external input (V5, established Phase 1) |
| T-04-05 | Information Disclosure | Numeric coercion of predictor columns | low | mitigate | Reuse `pd.to_numeric(..., errors="coerce")` (panel_base precedent) for any new numeric column touched |
| T-04-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs in this plan — all libraries (`linearmodels`, `numpy`, `pandas`) are pre-existing pinned deps; nothing new to install |

Per RESEARCH.md Security Domain: this is a local single-user academic pipeline — no auth/session/crypto/multi-user surface. No `.pkl` is written by this plan (serialization lives in Plan 03).
</threat_model>

<verification>
- `.venv/Scripts/python.exe -m pytest tests/test_simulate.py -q` — all four tests green.
- `.venv/Scripts/python.exe -m pytest tests/ -q` — full suite still green (no regression to panel_base tests).
- No modification to `src/panel_base.py` (git diff shows it unchanged).
</verification>

<success_criteria>
src/simulate.py exposes `resample_entities`, `bootstrap_counterfactual`, `check_non_extrapolation`, `fit_interaction_model` — all dependent-variable-agnostic (D-12), all covered by passing fixture-based unit tests, with bootstrap determinism (REPRO-02) and non-extrapolation-by-exclusion (D-04) proven.
</success_criteria>

<output>
Create `.planning/phases/04-interpretabilidad-simulaci-n-y-robustez/04-01-SUMMARY.md` when done.
</output>
