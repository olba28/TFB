---
phase: 04-interpretabilidad-simulaci-n-y-robustez
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - notebook/4_1_interpretabilidad_simulacion.ipynb
  - data/modelos/rf_shap_model.pkl
  - scripts/verify_repro02.py
autonomous: false
requirements: [INTERP-01, INTERP-02, INTERP-03, INTERP-04, INTERP-05, INTERP-06, REPRO-02]

must_haves:
  truths:
    - "The notebook executes top-to-bottom without error against the real data/panel.db (nbconvert --execute)"
    - "A multi-scenario sensitivity plot renders at least three reduction scenarios (-10/-20/-30%) each with its bootstrap percentile CI band (INTERP-01/02)"
    - "Per-scenario excluded-country counts/lists from the non-extrapolation check are documented explicitly in the notebook (D-04)"
    - "The heterogeneity section renders only grouped interaction-coefficient tables (region, is_ldc) with SE/CI — never a per-country prediction (INTERP-03/D-11)"
    - "A VIF/correlation table with the real Phase-2 numbers precedes the SHAP output, with an explicit correlation-bias caveat (INTERP-04)"
    - "Partial-dependence plots complement SHAP for the correlated predictors (INTERP-05)"
    - "The RF's oob_score_ is reported as the predictive reference metric (INTERP-06)"
    - "rf_shap_model.pkl is serialized and round-trip-verified (D-08)"
    - "Two full top-to-bottom executions produce bit-identical bootstrap CIs, RF feature importances, and SHAP values (REPRO-02)"
  artifacts:
    - notebook/4_1_interpretabilidad_simulacion.ipynb
    - data/modelos/rf_shap_model.pkl
    - scripts/verify_repro02.py
  key_links:
    - "notebook imports src.simulate and src.interpret and calls their public functions unmodified"
    - "SEED=42 threaded through every stochastic step (bootstrap SeedSequence + RF random_state)"
    - "rf returned by interpret.shap_analysis is serialized via the model1_gdp.pkl round-trip pattern"
---

<objective>
Build `notebook/4_1_interpretabilidad_simulacion.ipynb` (D-13): the single Phase-4 orchestration notebook that runs, in order, the counterfactual simulation (INTERP-01/02), the interaction-term heterogeneity analysis (INTERP-03), and the RF/SHAP/PDP interpretability stack (INTERP-04/05/06) against the real `data/panel.db`, serializes `rf_shap_model.pkl` (D-08), and proves end-to-end reproducibility (REPRO-02).

Purpose: Turn the Plan 01/02 primitives into the tribunal-facing narrative and the pre-computed artifacts Phase 5's dashboard consumes without live recomputation (DASH-01).
Output: `notebook/4_1_interpretabilidad_simulacion.ipynb`, `data/modelos/rf_shap_model.pkl`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-interpretabilidad-simulaci-n-y-robustez/04-CONTEXT.md
@.planning/phases/04-interpretabilidad-simulaci-n-y-robustez/04-RESEARCH.md
@.planning/phases/04-interpretabilidad-simulaci-n-y-robustez/04-PATTERNS.md
@.planning/phases/04-interpretabilidad-simulaci-n-y-robustez/04-VALIDATION.md
@notebook/3_1_modelo1_pib.ipynb
@src/simulate.py
@src/interpret.py
@src/panel_base.py
@src/db.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Simulation + heterogeneity notebook sections</name>
  <files>notebook/4_1_interpretabilidad_simulacion.ipynb</files>
  <read_first>notebook/3_1_modelo1_pib.ipynb (cell 1 sys.path/chdir bootstrap, cell 3 drift-check + `display(Markdown(...))` alert, cells 5-6 provisional-fit→Pesaran→choose_cov_type), 04-PATTERNS.md notebook section, src/simulate.py (Plan 01 public signatures), 04-RESEARCH.md Pattern 1/2 + Code Examples "Multi-scenario plot".</read_first>
  <action>Create the notebook with the Phase-3 bootstrap cell (sys.path insert + `PROJECT_ROOT`, `engine = db.get_engine(...)`, imports `from src import db, panel_base, simulate, interpret`), constants `DEP_VAR="8.1.1"`, `INDEP_VARS=["6.4.2"]`, `SEED=42` (REPRO-02, single project-wide constant threaded everywhere). Load `panel_clean`/`panel_exclusions` via `pd.read_sql` and `panel_base.filter_by_exclusions`, reusing the drift-check (expected ~171 countries / ~3879 obs) with the `display(Markdown(...))` alert on >10% drift. Load `data/modelos/model1_gdp.pkl` read-only as the fitted baseline. Simulation section: call `simulate.bootstrap_counterfactual(fitted_results, filtered, DEP_VAR, "6.4.2", reduction_pcts=[-0.10,-0.20,-0.30], baseline_year=2022, n_replicas=1000, seed=SEED)` (D-01/D-02/D-03), then render a multi-scenario sensitivity plot (INTERP-02) showing all three scenarios with their 2.5/97.5 percentile CI bands, framed in a markdown cell explicitly as a SENSITIVITY analysis, not causal prediction. Document each scenario's excluded-country count/list from the non-extrapolation check using the drift-alert Markdown convention (D-04 "debe documentarse explícitamente"). Heterogeneity section: call `simulate.fit_interaction_model` twice — once with `group_col="region"`, once with `group_col="is_ldc"` (D-10) — and render ONLY grouped coefficient/SE/CI tables (D-11), with a markdown note that no per-country prediction is produced (INTERP-03). For dev iteration the executor may temporarily lower `n_replicas`, but the COMMITTED notebook must use 1000 and re-execute fully before commit.</action>
  <verify>
    <automated>.venv/Scripts/python.exe -m jupyter nbconvert --to notebook --execute --inplace notebook/4_1_interpretabilidad_simulacion.ipynb</automated>
  </verify>
  <done>Notebook executes without error; simulation section produces a ≥3-scenario CI plot and documents per-scenario excluded countries; heterogeneity section shows region and is_ldc interaction-coefficient tables with SE/CI and no per-country prediction.</done>
</task>

<task type="auto">
  <name>Task 2: Interpretability section + serialize rf_shap_model.pkl</name>
  <files>notebook/4_1_interpretabilidad_simulacion.ipynb, data/modelos/rf_shap_model.pkl</files>
  <read_first>notebook/3_1_modelo1_pib.ipynb cells 9-10 (serialize + round-trip-verify pattern), src/interpret.py (Plan 02 signatures), 04-RESEARCH.md Pattern 3 + Pitfall #4/#5 (real Phase-2 VIF numbers), notebook/2_1_construccion_panel_eda.ipynb cells 13-15 (real VIF/correlation numbers to reproduce honestly).</read_first>
  <action>Add the interpretability section AFTER heterogeneity (D-13 order). First render `interpret.compute_vif_table` + a correlation matrix for the RF's numeric predictors (`6.4.2`, `6.4.1`, `8.2.1`) BEFORE any SHAP output (INTERP-04 precedence), reporting the honest Phase-2 numbers (max |r|≈0.09 among predictors, global VIF<2.2) with an explicit markdown caveat that SHAP's correlation-bias applies in principle and that regional-subgroup VIF is much higher for some regions (Pitfall #5 — report real numbers, do not force a "highly correlated" narrative). Call `interpret.shap_analysis(panel_clean, DEP_VAR, feature_vars, seed=SEED)` (D-05 multivariate predictors; `2.3.1` excluded per D-06), render `shap.summary_plot`, report `rf.oob_score_` as the predictive reference metric (INTERP-06, D-07 same RF), and render `interpret.partial_dependence_plots` for the correlated predictors (INTERP-05). Serialize the returned RF to `data/modelos/rf_shap_model.pkl` (D-08) using the Phase-3 pattern verbatim: `pickle.dump` → reload → assert `np.allclose(reloaded.feature_importances_, rf.feature_importances_)` (round-trip fidelity, "never trust an unverified pickle"). The `.pkl` is produced and consumed only by this project's own code — never loaded from an untrusted/external path (matches `model1_gdp.pkl` invariant, T-04-01 mitigation).</action>
  <verify>
    <automated>.venv/Scripts/python.exe -m jupyter nbconvert --to notebook --execute --inplace notebook/4_1_interpretabilidad_simulacion.ipynb && .venv/Scripts/python.exe -c "import pickle,numpy as np; rf=pickle.load(open('data/modelos/rf_shap_model.pkl','rb')); print('oob', float(rf.oob_score_)); assert np.isfinite(rf.oob_score_)"</automated>
  </verify>
  <done>VIF/correlation table renders with the real Phase-2 numbers before SHAP output, with an explicit correlation-bias caveat; SHAP summary plot and PDP plots render; oob_score_ is reported (INTERP-06); `data/modelos/rf_shap_model.pkl` exists and round-trips (`np.allclose` on `feature_importances_`, matching the `model1_gdp.pkl` precedent).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2b: Confirm notebook narrative and outputs against Phase-4 success criteria</name>
  <what-built>A complete Phase-4 notebook (simulation + heterogeneity + SHAP/VIF/PDP sections) executed top-to-bottom against the real 171-country panel, plus a serialized `data/modelos/rf_shap_model.pkl`.</what-built>
  <action>HALT for a blocking human verification: pause and present the executed notebook for the human to confirm against the how-to-verify steps below. Do not proceed to Task 3 until approved.</action>
  <how-to-verify>
    1. Open `notebook/4_1_interpretabilidad_simulacion.ipynb` in Jupyter Lab or VS Code.
    2. Confirm the multi-scenario sensitivity plot (INTERP-02) shows three visibly distinct CI bands (-10%/-20%/-30%) with axis and legend labels, and a markdown cell explicitly framing this as sensitivity analysis, not causal prediction.
    3. Confirm the heterogeneity section (region, is_ldc) shows only grouped coefficient/SE/CI tables — scroll the whole section and confirm no per-country predicted value appears anywhere (INTERP-03/D-11).
    4. Confirm the VIF/correlation table appears BEFORE the SHAP plots, with numbers that match `notebook/2_1_construccion_panel_eda.ipynb`'s Phase-2 output (not a generic/templated disclaimer), and an explicit correlation-bias caveat sentence (INTERP-04).
    5. Confirm at least one PDP plot exists per RF numeric predictor flagged as correlated in Phase 2, with a brief rationale (INTERP-05).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Prove full-notebook reproducibility (REPRO-02)</name>
  <files>notebook/4_1_interpretabilidad_simulacion.ipynb, scripts/verify_repro02.py</files>
  <read_first>04-VALIDATION.md "Manual-Only Verifications" (full-notebook reproducibility row), 04-RESEARCH.md Pitfall #2 (n_jobs non-determinism) and Open Questions Q2 (runtime budget, ≈25-30 min per full run).</read_first>
  <action>Add a final notebook cell (before the last markdown wrap-up) that serializes a small reproducibility snapshot — a dict with each scenario's `ci_2.5`/`ci_97.5` arrays and `excluded_countries` list, the RF's `feature_importances_` and `oob_score_`, and the SHAP values array — to a fixed path `data/modelos/_repro_snapshot.pkl` (gitignored working artifact, overwritten every execution, NOT the same file as `rf_shap_model.pkl`). Create `scripts/verify_repro02.py`: a standalone script (not a pytest test — too slow for the routine `pytest tests/ -q` suite) that (1) runs `jupyter nbconvert --to notebook --execute` on the committed notebook into a throwaway temp path, loads the resulting `_repro_snapshot.pkl` as "run A"; (2) repeats the same execution into a second throwaway temp path, loads `_repro_snapshot.pkl` again as "run B" (each execution overwrites the same fixed snapshot path, so read it immediately after each run before the next overwrites it); (3) asserts `np.array_equal` (exact, not `pytest.approx`) between run A and run B for every array/list captured, printing a clear PASS/FAIL summary and exiting non-zero on any mismatch. If a mismatch is found, the root cause is almost certainly a missed `n_jobs=1`/unseeded step (Pitfall #2) — fix `src/simulate.py`/`src/interpret.py` (not the notebook), then re-run the script. Neither throwaway execution's notebook copy is committed to git; only the committed notebook (already executed in Task 1/2) and `scripts/verify_repro02.py` are checked in.</action>
  <verify>
    <automated>.venv/Scripts/python.exe scripts/verify_repro02.py</automated>
  </verify>
  <done>`scripts/verify_repro02.py` exits 0, printing that two independent top-to-bottom executions of the notebook against the real panel produced bit-identical bootstrap CIs, excluded-country lists, RF feature importances/oob_score_, and SHAP values; the committed notebook itself reflects the Task 1/2 execution (not either throwaway comparison run).</done>
</task>

</tasks>

## Artifacts this phase produces

**New files:**
- `notebook/4_1_interpretabilidad_simulacion.ipynb` — single Phase-4 orchestration notebook (D-13): loads `model1_gdp.pkl`, runs simulation, heterogeneity, SHAP/VIF/PDP sections in order, serializes `rf_shap_model.pkl`
- `data/modelos/rf_shap_model.pkl` — serialized `RandomForestRegressor` (D-08), consumed unmodified by Phase 5 (dashboard) and reusable by Phase 6 (`dep_var="2.3.1"`)
- `scripts/verify_repro02.py` — standalone reproducibility-proof script: executes the notebook twice, diffs bootstrap CIs/excluded countries/RF importances/oob_score_/SHAP values (REPRO-02 end-to-end gate)

**Symbols consumed from Plan 01/02 (not redefined here):** `simulate.bootstrap_counterfactual`, `simulate.fit_interaction_model`, `simulate.check_non_extrapolation`, `interpret.shap_analysis`, `interpret.compute_vif_table`, `interpret.partial_dependence_plots`, `panel_base.fit_panel_model`, `panel_base.filter_by_exclusions`, `db.get_engine`.

**Notebook-local constants:** `DEP_VAR = "8.1.1"`, `INDEP_VARS = ["6.4.2"]`, `SEED = 42`, `PROJECT_ROOT`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite → notebook | Reads own `panel_clean`/`panel_exclusions` (validated Phases 1–2); no external/user input |
| notebook → `data/modelos/rf_shap_model.pkl` | Local file write/read-back, never a network or user-supplied path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03 | Tampering | `pickle.load`/`pickle.dump` on `rf_shap_model.pkl` | high | mitigate | Follow the exact `model1_gdp.pkl` precedent (Phase 3): the `.pkl` is only ever produced and consumed by this project's own notebook code, never loaded from an untrusted/external/user-supplied path or network location; round-trip-verified immediately after write via `np.allclose` on `feature_importances_` |
| T-04-04 | Repudiation | Non-reproducible full-notebook run (bootstrap + RF combined) | medium | mitigate | Task 3 explicitly re-executes the full notebook twice and diffs bootstrap CIs / RF importances / SHAP values bit-for-bit (REPRO-02 end-to-end proof, beyond the unit-fixture determinism already covered in Plan 01/02) |
| T-04-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new installs in this plan — `interpret.partial_dependence_plots` (Plan 02) already resolved INTERP-05 with zero new dependencies |

Per RESEARCH.md Security Domain: local single-user academic notebook — no auth/session/crypto/multi-user surface.
</threat_model>

<verification>
- `.venv/Scripts/python.exe -m jupyter nbconvert --to notebook --execute --inplace notebook/4_1_interpretabilidad_simulacion.ipynb` completes without error.
- `data/modelos/rf_shap_model.pkl` exists, loads, and round-trips (`np.allclose` on `feature_importances_`).
- Task 3's second, independent full execution confirms bit-identical bootstrap CIs, RF feature importances/oob_score_, and SHAP values (REPRO-02).
- `.venv/Scripts/python.exe -m pytest tests/ -q` — full suite still green (no regression from notebook-adjacent changes).
</verification>

<success_criteria>
The Phase-4 notebook runs end-to-end against the real panel, delivering all six ROADMAP Phase-4 success criteria: a ≥3-scenario bootstrap sensitivity plot with documented non-extrapolation exclusions (1–2), a heterogeneity coefficient table with no per-country prediction (3), a VIF-preceded SHAP analysis with an honest correlation-bias caveat (4), complementary PDP plots (5), and proven bit-identical reproducibility across two full top-to-bottom runs (6). `rf_shap_model.pkl` is serialized and ready for Phase 5/6 reuse.
</success_criteria>

<output>
Create `.planning/phases/04-interpretabilidad-simulaci-n-y-robustez/04-03-SUMMARY.md` when done.
</output>
