---
phase: 04-interpretabilidad-simulaci-n-y-robustez
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/interpret.py
  - tests/test_interpret.py
autonomous: true
requirements: [INTERP-04, INTERP-05, INTERP-06, REPRO-02]

must_haves:
  truths:
    - "shap_analysis trains a RandomForestRegressor with n_jobs=1 and random_state=SEED and returns SHAP values of shape (n_samples, n_features) via shap.TreeExplainer"
    - "The trained RF exposes a real-float oob_score_ (INTERP-06 predictive reference metric, D-07 same RF)"
    - "compute_vif_table returns a VIF table (statsmodels variance_inflation_factor) computed and reportable BEFORE SHAP output (INTERP-04 correlation-bias precedence)"
    - "partial_dependence_plots produces PDP figures for the correlated predictors without any new dependency (INTERP-05, sklearn fallback)"
    - "Two RF trainings with the same seed and n_jobs=1 give identical feature_importances_/oob_score_ (REPRO-02, Pitfall #2)"
  artifacts:
    - src/interpret.py
    - tests/test_interpret.py
  key_links:
    - "shap_analysis returns the fitted rf so Plan 03's notebook can serialize it to rf_shap_model.pkl (D-08)"
    - "RF predictors are multivariate: 6.4.2, 6.4.1, 8.2.1, is_ldc, is_lldc, is_sids, region one-hot (D-05); 2.3.1 excluded (D-06)"
    - "compute_vif_table reuses statsmodels VIF, not a hand-rolled implementation"
---

<objective>
Build `src/interpret.py`: the parametric ML-interpretability module — a shared multivariate RandomForest feeding SHAP (INTERP-04), partial-dependence plots (INTERP-05), and its own out-of-bag score as the predictive reference metric (INTERP-06), preceded by a VIF/correlation table. Fully reproducible with `n_jobs=1` (REPRO-02). Test-first.

Purpose: Deliver the interpretability primitives the Phase-4 notebook (Plan 03) orchestrates and serializes, designed so Phase 6 can reuse them with `dep_var="2.3.1"` unmodified (D-12).
Output: `src/interpret.py`, `tests/test_interpret.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-interpretabilidad-simulaci-n-y-robustez/04-CONTEXT.md
@.planning/phases/04-interpretabilidad-simulaci-n-y-robustez/04-RESEARCH.md
@.planning/phases/04-interpretabilidad-simulaci-n-y-robustez/04-PATTERNS.md
@.planning/phases/04-interpretabilidad-simulaci-n-y-robustez/04-VALIDATION.md
@src/panel_base.py
@tests/test_panel_base.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing unit tests for src/interpret.py</name>
  <files>tests/test_interpret.py</files>
  <read_first>tests/test_panel_base.py (fixture style, `np.random.default_rng`, docstring-per-test), 04-RESEARCH.md Pattern 3 (live-verified RF+SHAP code) + Pitfall #2 (`n_jobs=-1` non-determinism), 04-VALIDATION.md Per-Task Verification Map + Wave 0 Requirements.</read_first>
  <behavior>
    - test_shap_values_shape (INTERP-04): `shap_analysis` on a tiny synthetic fixture returns SHAP values with shape `(n_samples, n_features)` matching the one-hot-encoded feature matrix.
    - test_oob_score_present (INTERP-06): the returned RF exposes `oob_score_` as a real float (not NaN/None).
    - test_rf_determinism (REPRO-02): fitting the RF twice with `n_jobs=1, random_state=SEED` on the same fixture yields `np.array_equal` `feature_importances_` — directly guards Pitfall #2.
    - test_vif_table (INTERP-04): `compute_vif_table` returns a table with one VIF row per numeric predictor, all finite for a non-collinear fixture.
  </behavior>
  <action>Create `tests/test_interpret.py` importing `from src import interpret`. Build a small synthetic fixture DataFrame (~40–60 rows, a handful of countries) carrying the RF predictor columns (`6.4.2`, `6.4.1`, `8.2.1`, `is_ldc`, `is_lldc`, `is_sids`, `region`) and a dependent column with a known linear-ish relationship — analogous to `_make_synthetic_panel` but WITHOUT panel structure (RF training is not panel-indexed). Write the four tests in <behavior>. Tests MUST fail at this point because `src/interpret.py` does not yet exist (RED). Do NOT load the real `data/panel.db` in unit tests (Wave 0: fast fixtures only). Column names with dots (`6.4.2`) are strings — quote them consistently. Docstrings cite D-05 (multivariate predictors), D-07 (same RF for INTERP-06), REPRO-02.</action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_interpret.py -q 2>&1 | grep -E "error|ModuleNotFoundError|failed"</automated>
  </verify>
  <done>tests/test_interpret.py exists with four tests + a non-panel synthetic fixture; collection fails only because `src.interpret` cannot be imported (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement RF + SHAP + VIF in src/interpret.py</name>
  <files>src/interpret.py</files>
  <read_first>04-RESEARCH.md Pattern 3 + Pitfall #2/#4/#5 + "Don't Hand-Roll" VIF row, 04-PATTERNS.md `src/interpret.py` section, src/panel_base.py (docstring + `warnings.warn(stacklevel=2)` conventions).</read_first>
  <action>Create `src/interpret.py` with `from __future__ import annotations`, a module docstring citing D-05/D-06/D-07/D-08/D-12, and typed imports. Implement: (1) `compute_vif_table(df, numeric_cols)` — reuse `statsmodels.stats.outliers_influence.variance_inflation_factor` (do NOT hand-roll VIF math; this mirrors Phase 2's `2_1_construccion_panel_eda.ipynb` helper), returning a tidy DataFrame of one VIF row per numeric predictor. (2) `shap_analysis(df, dep_var, feature_vars, seed=42)` — select complete-case rows via `.dropna()` on `feature_vars + [dep_var]`, one-hot encode `region` with `pd.get_dummies(..., columns=["region"], drop_first=True)`, train `RandomForestRegressor(n_estimators=300, random_state=seed, n_jobs=1, oob_score=True)` — `n_jobs` MUST be 1 (Pitfall #2: `n_jobs=-1` breaks REPRO-02 even with a fixed random_state, live-verified), compute `shap.TreeExplainer(rf).shap_values(X)`, and RETURN the fitted `rf`, the SHAP values, the feature matrix `X`, and the explainer so the caller (notebook) can serialize the RF (D-08) and read `rf.oob_score_` for INTERP-06 (D-07 — the SAME RF, no separate GBM). Predictors are `6.4.2`, `6.4.1`, `8.2.1` + typology (`is_ldc`, `is_lldc`, `is_sids`, `region`) per D-05; `2.3.1` is excluded per D-06. Docstring notes that during iterative dev `check_additivity=False` can be passed to speed the SHAP call (Pitfall #4), but the committed default keeps the full check. Emit a `UserWarning(stacklevel=2)` if VIF is undefined (perfect collinearity).</action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_interpret.py::test_shap_values_shape tests/test_interpret.py::test_oob_score_present tests/test_interpret.py::test_rf_determinism tests/test_interpret.py::test_vif_table -x -q</automated>
  </verify>
  <done>shap_analysis returns a deterministic (n_jobs=1) RF with a real oob_score_ and SHAP values of shape (n_samples, n_features); compute_vif_table returns finite VIFs; same-seed RF trainings are bit-identical.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement partial-dependence plots (ALE complement) in src/interpret.py</name>
  <files>src/interpret.py</files>
  <read_first>04-RESEARCH.md §Standard Stack "Alternatives Considered" (PyALE vs sklearn PDP) + §Package Legitimacy Audit, 04-CONTEXT.md Claude's Discretion (ALE/PDP variable choice), CLAUDE.md (dependencies fixed — no new deps without justification).</read_first>
  <action>Add `partial_dependence_plots(rf, X, features, ax=None)` to `src/interpret.py` using `sklearn.inspection.PartialDependenceDisplay.from_estimator` — the zero-new-dependency fallback (INTERP-05). Chosen over `PyALE` deliberately: PyALE is `SUS`-flagged in the legitimacy audit and CLAUDE.md fixes dependencies; the RF's three numeric predictors are only weakly correlated globally (max |r|=0.09, VIF<2.2 per Phase 2), which blunts PDP's correlated-feature bias concern for this specific dataset — document that rationale in the function docstring so the tribunal framing is explicit. The function renders one PDP per requested feature (caller passes the correlated Phase-2 predictors) and returns the display/axes for the notebook to embed. NO `pip install` and NO checkpoint task are required because no new dependency is introduced. Docstring notes that if true ALE is later desired, PyALE would require a `checkpoint:human-verify` gate before install (per the legitimacy protocol) — but that path is not taken here.</action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_interpret.py -q</automated>
  </verify>
  <done>partial_dependence_plots renders PDP figures for the given features using only already-pinned scikit-learn; full `tests/test_interpret.py` passes; no new entry added to requirements.txt.</done>
</task>

</tasks>

## Artifacts this phase produces

**New files:**
- `src/interpret.py` — parametric ML-interpretability module (D-12), reusable unmodified by Phase 6 with `dep_var="2.3.1"`
- `tests/test_interpret.py` — fixture-based unit tests for the above

**New functions (in `src/interpret.py`):**
- `compute_vif_table(df, numeric_cols) -> pd.DataFrame` — VIF per numeric predictor, via `statsmodels.stats.outliers_influence.variance_inflation_factor` (INTERP-04 precedence check)
- `shap_analysis(df, dep_var, feature_vars, seed=42) -> tuple` — trains `RandomForestRegressor(n_jobs=1, oob_score=True)`, returns `(rf, shap_values, X, explainer)` (INTERP-04/06, D-05/D-06/D-07)
- `partial_dependence_plots(rf, X, features, ax=None)` — PDP plots via `sklearn.inspection.PartialDependenceDisplay` (INTERP-05, zero new dependency)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite → interpret.py | Reads own `panel_clean` (validated Phases 1–2); no external/user input |
| pip registry → .venv | INTERP-05 could introduce a new dependency — deliberately avoided (sklearn PDP fallback) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-02 | Tampering | Numeric predictor columns from panel_clean | low | mitigate | Complete-case `.dropna()` + rely on Phase-1/2 validation; reuse existing typed columns, no external input |
| T-04-06 | Repudiation | Non-reproducible RF (n_jobs parallelism) | medium | mitigate | Enforce `n_jobs=1` + fixed `random_state`; test_rf_determinism guards it (Pitfall #2) |
| T-04-SC | Tampering | npm/pip/cargo installs | low | mitigate | INTERP-05 uses `sklearn.inspection.PartialDependenceDisplay` (already pinned) — the `SUS`-flagged `PyALE` is NOT installed, so no `[ASSUMED]`/`[SUS]` package enters the tree; zero new deps, no checkpoint needed |

Per RESEARCH.md Security Domain: local single-user academic pipeline — no auth/session/crypto/multi-user surface. This plan trains the RF but does NOT serialize it (`rf_shap_model.pkl` is written in Plan 03, following the `model1_gdp.pkl` round-trip precedent).
</threat_model>

<verification>
- `.venv/Scripts/python.exe -m pytest tests/test_interpret.py -q` — all four tests green.
- `.venv/Scripts/python.exe -m pytest tests/ -q` — full suite still green.
- `git diff requirements.txt` shows no change (no new dependency added; PyALE not installed).
</verification>

<success_criteria>
src/interpret.py exposes `compute_vif_table`, `shap_analysis`, `partial_dependence_plots` — all dependent-variable-agnostic (D-12), covered by passing fixture tests, with RF determinism under `n_jobs=1` (REPRO-02) and a real `oob_score_` for INTERP-06 (D-07), and INTERP-05 satisfied with zero new dependencies.
</success_criteria>

<output>
Create `.planning/phases/04-interpretabilidad-simulaci-n-y-robustez/04-02-SUMMARY.md` when done.
</output>
