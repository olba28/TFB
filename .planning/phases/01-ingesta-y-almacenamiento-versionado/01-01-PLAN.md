---
phase: 01-ingesta-y-almacenamiento-versionado
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - requirements-dev.txt
  - requirements.lock.txt
  - pyproject.toml
  - src/ingesta/__init__.py
  - tests/__init__.py
  - tests/conftest.py
  - tests/ingesta/__init__.py
  - tests/fixtures/.gitkeep
autonomous: false
requirements: [REPRO-01]
user_setup:
  - service: none
    why: "Human legitimacy sanity-check of pytest/pytest-cov before pip install (SUS heuristic flag from RESEARCH Package Legitimacy Audit)"
must_haves:
  truths:
    - "A project-scoped .venv exists and requirements.lock.txt reflects its pip freeze (REPRO-01)"
    - "Downloaded raw JSON and data/panel.db are git-ignored; per-indicator manifests are NOT ignored"
    - "pytest can discover and run tests/ against the new .venv"
  artifacts:
    - ".gitignore (renamed from gitignore, active)"
    - "requirements-dev.txt"
    - "requirements.lock.txt"
    - "pyproject.toml with [tool.pytest.ini_options]"
    - "src/ingesta/__init__.py, tests/__init__.py, tests/conftest.py, tests/ingesta/__init__.py"
  key_links:
    - ".gitignore negation pattern must match the actual {fecha}.manifest.json filename, not a bare manifest.json"
    - "pip freeze must run inside .venv, not the global interpreter (or lockfile captures unrelated system packages)"
---

<objective>
Establish the reproducibility and test foundation for Phase 1 before any ingestion code is written: fix the inactive `.gitignore`, create an isolated `.venv`, freeze exact dependency versions (REPRO-01), and stand up the pytest scaffolding every downstream plan's tests depend on.

Purpose: RESEARCH flagged three hard blockers — no `.venv` exists (a global `pip freeze` would pollute the lockfile), `pytest`/`pytest-cov` are not installed (blocking the D-03 mocked test), and the repo's ignore file is checked in as plain `gitignore` (no leading dot) so `git check-ignore` confirms raw JSON is NOT ignored. All three must be cleared first.
Output: An active `.gitignore`, `.venv` with pinned `requirements.lock.txt`, `requirements-dev.txt`, `pyproject.toml` pytest config, and empty `tests/` + `src/ingesta/` package skeletons.
</objective>

<artifacts_produced>
This plan creates the following symbols/paths (shared across the phase):
- `.gitignore` (renamed + extended: `data/panel.db`, corrected `!data/**/*.manifest.json` negation)
- `requirements-dev.txt` (pytest, pytest-cov)
- `requirements.lock.txt` (via `pip freeze` inside `.venv`)
- `pyproject.toml` → `[tool.pytest.ini_options]` with `testpaths = ["tests"]`
- `src/ingesta/__init__.py` (package init, consumed by Plans 02/03/04/05)
- `tests/__init__.py`, `tests/ingesta/__init__.py`, `tests/conftest.py`, `tests/fixtures/`
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ingesta-y-almacenamiento-versionado/01-CONTEXT.md
@.planning/phases/01-ingesta-y-almacenamiento-versionado/01-RESEARCH.md

# Current (misnamed, inactive) ignore file to migrate — DO NOT re-author from scratch, preserve its rules:
@gitignore
@requirements.txt
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify pytest/pytest-cov legitimacy before install</name>
  <read_first>
    - .planning/phases/01-ingesta-y-almacenamiento-versionado/01-RESEARCH.md § "Package Legitimacy Audit"
  </read_first>
  <what-built>
    Nothing installed yet. RESEARCH's Package Legitimacy Audit flagged `pytest` and `pytest-cov` as `[SUS]` — but this traces to the tool's release-recency heuristic (it checked latest-release timestamps, not first-publish dates), not any real red flag. Both are the de-facto standard Python test tooling with public repos and no postinstall scripts. This is a one-line sanity check, not a deep investigation.
  </what-built>
  <action>HALT for a blocking human legitimacy check: pause before any `pip install` of pytest/pytest-cov until the human confirms both packages are legitimate (see how-to-verify). Do not install until approved.</action>
  <how-to-verify>
    1. Open https://pypi.org/project/pytest/ — confirm it is the well-known pytest-dev project (millions of downloads, repo github.com/pytest-dev/pytest).
    2. Open https://pypi.org/project/pytest-cov/ — confirm repo github.com/pytest-dev/pytest-cov.
    3. Confirm you are comfortable installing both into the project `.venv`.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with install, or name an alternative.</resume-signal>
  <acceptance_criteria>Human has confirmed pytest + pytest-cov are legitimate and approved for install.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Fix repo hygiene — activate and extend .gitignore</name>
  <files>.gitignore</files>
  <read_first>
    - gitignore (the current misnamed file — its CONTENTS are correct and must be preserved)
    - .planning/phases/01-ingesta-y-almacenamiento-versionado/01-RESEARCH.md § "Common Pitfalls" Pitfall 3 and § "Open Questions" Q1
  </read_first>
  <action>
    Rename the checked-in `gitignore` file to `.gitignore` (git mv so history is preserved), keeping ALL existing rules verbatim. Then apply two corrections RESEARCH identified: (1) add a line ignoring the SQLite database `data/panel.db` (D-09) — currently not covered by any rule and Open Question Q1 recommends git-ignoring it since it is fully regenerable from `data/raw/` + `src/`; (2) fix the manifest-negation bug: the current `!data/**/manifest.json` re-include pattern does NOT match the actual manifest filenames, which per D-06 are `{fecha}.manifest.json` (e.g. `2026-07-10.manifest.json`) — replace it with `!data/**/*.manifest.json` so provenance manifests stay tracked while raw JSON stays ignored. Do NOT broaden the ignore to cover `src/ingesta/data/*.csv` — the M49 reference CSV (Plan 03) lives under `src/` (not `data/`) and must remain committed; the anchored `data/**/*.csv` rule already excludes it, but confirm with git check-ignore. After editing, verify with `git check-ignore` that `data/raw/6.4.2/2026-07-10.json` IS ignored, `data/panel.db` IS ignored, and `data/raw/6.4.2/2026-07-10.manifest.json` is NOT ignored.
  </action>
  <verify>
    <automated>git check-ignore -q data/raw/6.4.2/2026-07-10.json && git check-ignore -q data/panel.db && ! git check-ignore -q data/raw/6.4.2/2026-07-10.manifest.json && test -f .gitignore && echo OK</automated>
  </verify>
  <acceptance_criteria>`.gitignore` exists and is active; raw JSON and `data/panel.db` are ignored; `{fecha}.manifest.json` files are tracked; the old plain `gitignore` file no longer exists.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Create .venv, freeze lockfile, and stand up pytest scaffolding</name>
  <files>requirements-dev.txt, requirements.lock.txt, pyproject.toml, src/ingesta/__init__.py, tests/__init__.py, tests/ingesta/__init__.py, tests/conftest.py, tests/fixtures/.gitkeep</files>
  <read_first>
    - .planning/phases/01-ingesta-y-almacenamiento-versionado/01-RESEARCH.md § "Environment Availability" and § "Validation Architecture" (Test Framework table + Wave 0 Gaps)
    - requirements.txt (existing runtime deps to install into the venv)
  </read_first>
  <action>
    Create an isolated virtual environment at `.venv` via `python -m venv .venv` (already git-ignored). Install the existing runtime dependencies into it (`.venv/Scripts/python.exe -m pip install -r requirements.txt` on Windows). Create `requirements-dev.txt` pinning the dev-only test tooling with conservative floors — `pytest>=8` and `pytest-cov>=5` (do not add them to `requirements.txt`; they are dev-only per RESEARCH). Install dev deps into the same venv. Then generate `requirements.lock.txt` via `.venv/Scripts/python.exe -m pip freeze > requirements.lock.txt` — this MUST run against the venv interpreter, never the global one, or the lockfile captures unrelated system packages (REPRO-01). Create `pyproject.toml` with a `[tool.pytest.ini_options]` section setting `testpaths = ["tests"]`. Create the empty package/test skeleton: `src/ingesta/__init__.py`, `tests/__init__.py`, `tests/ingesta/__init__.py`, an empty `tests/conftest.py`, and a `tests/fixtures/.gitkeep` placeholder (the actual fixture JSON files are authored by the plans that use them). Do NOT commit `.venv/` (already ignored). Confirm `pytest` runs (collects zero tests cleanly) inside the venv.
  </action>
  <verify>
    <automated>test -s requirements.lock.txt && test -f requirements-dev.txt && test -f pyproject.toml && test -f src/ingesta/__init__.py && test -f tests/conftest.py && .venv/Scripts/python.exe -m pytest --collect-only -q; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>`.venv` exists; `requirements.lock.txt` is non-empty and produced by the venv's `pip freeze`; `requirements-dev.txt` pins pytest + pytest-cov; `pyproject.toml` has `testpaths = ["tests"]`; `pytest --collect-only` runs against the venv without errors (zero tests collected is expected).</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → local venv | Untrusted package code is installed and executed locally on install |
| local filesystem → git history | Files staged for commit may leak raw data dumps or local absolute paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | pip install pytest/pytest-cov | high | mitigate | Blocking human-verify checkpoint (Task 1) before install; both packages have public pytest-dev repos and no postinstall scripts (RESEARCH audit) |
| T-01-ID | Information Disclosure | git-tracked files under data/ and scripts | high | mitigate | `.gitignore` fix (Task 2) ignores raw JSON + `data/panel.db`; keeps only manifests; prevents accidental commit of bulk data / embedded local paths (RESEARCH Security Domain, Information Disclosure row) |
</threat_model>

<verification>
- `git check-ignore` confirms raw JSON + `panel.db` ignored, manifests tracked.
- `.venv/Scripts/python.exe -m pytest --collect-only` runs without import/config errors.
- `requirements.lock.txt` is non-empty and venv-scoped.
</verification>

<success_criteria>
REPRO-01 satisfied: `requirements.lock.txt` exists and reflects a clean, project-scoped `pip freeze`. Test infrastructure ready for Plans 02–05. Repo hygiene bug fixed before any data is fetched.
</success_criteria>

<output>
Create `.planning/phases/01-ingesta-y-almacenamiento-versionado/01-01-SUMMARY.md` when done.
</output>
