---
phase: 05-dashboard-y-preparaci-n-de-la-defensa
plan: 05
type: execute
wave: 4
depends_on: ["05-04"]
files_modified:
  - figuras/plan_b/README.md
autonomous: false
requirements: [DASH-05]
user_setup:
  - service: presentation-machine
    why: "DASH-05 requires the cold-cache rehearsal on the actual presentation machine (may differ from the dev machine)."
    dashboard_config:
      - task: "Confirm Chrome/Edge present (only if kaleido auto-capture is ever used; the manual browser-screenshot fallback needs no Chrome)"
        location: "Presentation machine — System browser"
must_haves:
  truths:
    - "With a cold Streamlit cache, `streamlit run src/dashboard/app.py` completes its first full render in a demo-acceptable time (target <5s), timed and documented (DASH-02/DASH-05)."
    - "Pre-rendered backup screenshots (PNG/PDF) of all 4 tabs with real data exist under figuras/plan_b/ and were rehearsed as the Plan B if the live dashboard fails (D-08/DASH-05)."
  artifacts:
    - figuras/plan_b/README.md
    - figuras/plan_b/ (4 tab captures with real data — human-produced)
  key_links:
    - "The rehearsal exercises the real data/panel.db + data/modelos/*.pkl through the full app built in 05-04 (not fixtures)."
---

<objective>
Complete DASH-05: rehearse the dashboard with a cold cache on the presentation machine (documenting the first-render time against the <5s demo target), and produce the pre-rendered Plan B backup — screenshots (PNG/PDF) of all four tabs with real data, saved under `figuras/plan_b/`, as the fallback if the live dashboard fails during the oral defense (D-08).

Purpose: The dashboard's whole reason for existing is a fluid, reliable live demo before the tribunal. This plan is the dress rehearsal + the safety net. Both items are inherently manual (wall-clock timing on real hardware; visual legibility of captures) and cannot be asserted by a unit test — they are the phase's human-verify gate.
Output: `figuras/plan_b/README.md` (index of captures + rehearsal notes) and the four tab captures.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dashboard-y-preparaci-n-de-la-defensa/05-CONTEXT.md
@.planning/phases/05-dashboard-y-preparaci-n-de-la-defensa/05-RESEARCH.md
@.planning/phases/05-dashboard-y-preparaci-n-de-la-defensa/05-UI-SPEC.md
@.planning/phases/05-dashboard-y-preparaci-n-de-la-defensa/05-VALIDATION.md
@src/dashboard/app.py
</context>

## Artifacts this phase produces (this plan)

- `figuras/plan_b/README.md` — index listing the four required tab captures, the recorded cold-start time, and rehearsal notes (documents DASH-05 evidence).
- `figuras/plan_b/*.png` (or `.pdf`) — one capture per tab, real data, produced by the human during the rehearsal checkpoint.

<tasks>

<task type="auto">
  <name>Task 1: Create figuras/plan_b/ index + rehearsal checklist</name>
  <read_first>
    - .planning/phases/05-dashboard-y-preparaci-n-de-la-defensa/05-UI-SPEC.md (Plan B Screenshot Contract — capture rules)
    - .planning/phases/05-dashboard-y-preparaci-n-de-la-defensa/05-VALIDATION.md (Manual-Only Verifications)
  </read_first>
  <action>
    Create `figuras/plan_b/README.md` documenting the Plan B backup contract so the evidence has a single home (mirrors the repo's existing `figuras/` convention for exported outputs). It must contain: (1) the list of the four required captures with exact filenames — `01_mapa_e_indicadores.png`, `02_modelo_1.png`, `03_simulacion.png`, `04_interpretabilidad_shap.png` — one per D-06 tab; (2) the UI-SPEC Plan B capture rules (capture after full render — no spinner/"Running…" visible; same maximized window size for all four; the Mapa tab captured with both choropleths on the SAME year); (3) a placeholder line "Cold-start time: __s (target <5s, cold cache, presentation machine)" to be filled during the rehearsal; (4) a note that `kaleido` is deliberately NOT installed (manual browser screenshot / Print-to-PDF is the chosen mechanism per RESEARCH Open Question 1 — no new dependency, matching the Phase-4 PyALE decision). Do not fabricate the captures or the timing here — those are produced by the human in Task 2/3.
  </action>
  <verify>
    <automated>test -f figuras/plan_b/README.md && grep -q "01_mapa_e_indicadores" figuras/plan_b/README.md && grep -q "Cold-start time" figuras/plan_b/README.md && echo OK</automated>
  </verify>
  <done>figuras/plan_b/README.md exists, lists the four capture filenames, the capture rules, and a cold-start-time placeholder.</done>
  <acceptance_criteria>
    - `figuras/plan_b/README.md` exists.
    - It names all four capture filenames (one per tab) and the UI-SPEC capture rules.
    - It contains a "Cold-start time" line to be filled at rehearsal and the note that kaleido is not installed.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Cold-cache rehearsal on the presentation machine (DASH-05)</name>
  <action>Human-verify checkpoint — no autonomous code change. The human runs the cold-cache rehearsal per the steps below and records the measured first-render time into figuras/plan_b/README.md.</action>
  <what-built>
    The full dashboard (`src/dashboard/app.py`, 4 tabs) reading the real `data/panel.db` and `data/modelos/*.pkl`, plus the `figuras/plan_b/README.md` capture index. All automated dashboard tests (`tests/dashboard`) are green.
  </what-built>
  <how-to-verify>
    Cold-cache rehearsal on the presentation machine (DASH-02/DASH-05):
    1. Clear Streamlit's cache: run `.venv/Scripts/python.exe -m streamlit cache clear` (or start a fresh process/machine so the cache is cold).
    2. Launch: `.venv/Scripts/python.exe -m streamlit run src/dashboard/app.py`. It must open on localhost (never a public address).
    3. Time the first full render to interactive (the sidebar shows "Carga en frío: N.NNs"). Confirm it is within the demo budget (target < 5s). If the Simulación/SHAP tabs' first cold compute pushes a tab beyond budget, note it — the demo-runtime `n_replicas` knob in `data.cached_bootstrap` can be lowered further, but do not change simulate.py's methodology.
    4. Click through all 4 tabs: confirm the Mapa tab shows two side-by-side choropleths with independent selectors and the Plotly play/slider animation across 2000–2022; confirm Modelo 1 table, Simulación scenario plot + metrics, and the SHAP summary all render.
    5. Record the measured cold-start time into `figuras/plan_b/README.md` (replace the placeholder).
  </how-to-verify>
  <resume-signal>Type "approved" with the recorded cold-start time, or describe issues (e.g. a tab exceeding the budget) to address.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Produce and verify the Plan B backup captures (DASH-05/D-08)</name>
  <action>Human-verify checkpoint — no autonomous code change. The human captures each of the 4 tabs to figuras/plan_b/ with the exact filenames and confirms each is legible with real data.</action>
  <what-built>
    The `figuras/plan_b/` capture index and the running dashboard from the rehearsal above.
  </what-built>
  <how-to-verify>
    Produce and verify the Plan B backup (D-08/DASH-05):
    1. With the dashboard fully rendered (no spinner), capture each of the 4 tabs to `figuras/plan_b/` using the exact filenames in the README, via browser screenshot or "Print to PDF" (maximized window, same size for all four; Mapa tab with both maps on the same year). No `kaleido`/Chrome dependency is required for manual capture.
    2. Open each saved capture and confirm it shows real (non-placeholder) data and is legible — all four tabs represented: "Mapa e indicadores", "Modelo 1", "Simulación", "Interpretabilidad (SHAP)".
    3. Confirm the captures would serve as a coherent walkthrough if the live dashboard failed mid-defense.
  </how-to-verify>
  <resume-signal>Type "approved" once all four legible, real-data captures exist under figuras/plan_b/, or describe what is missing.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| presentation machine→dashboard | The rehearsal runs the app on real hardware; binding must stay localhost. |
| (no new runtime code) | This plan adds documentation + human-produced captures only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-5-03 | Elevation of Privilege (network exposure) | `streamlit run` during rehearsal | low | mitigate | Rehearsal launch uses the default localhost binding; the checkpoint explicitly confirms it does not open on a public address (PROJECT.md local-only). |
| T-5-SC | Supply Chain | package installs | low | accept | Plan B uses manual browser capture — `kaleido` is deliberately NOT installed, so no package-legitimacy checkpoint is triggered this phase (RESEARCH Open Question 1). If a future decision reintroduces kaleido, a `checkpoint:human-verify` before `pip install kaleido` is required (kaleido was flagged `[SUS]` on unknown-downloads only). |
</threat_model>

<verification>
- `figuras/plan_b/README.md` exists with the four filenames + cold-start placeholder.
- Cold-cache rehearsal completed on the presentation machine, first-render time recorded (< 5s target).
- Four legible real-data tab captures exist under `figuras/plan_b/`.
- Full suite `.venv/Scripts/python.exe -m pytest tests/ -q` green before this gate.
</verification>

<success_criteria>
- DASH-05 satisfied: cold-cache rehearsal done and timed on the presentation machine; Plan B screenshots of all 4 tabs with real data rehearsed as the fallback (D-08).
</success_criteria>

<output>
Create `.planning/phases/05-dashboard-y-preparaci-n-de-la-defensa/05-05-SUMMARY.md` when done.
</output>
