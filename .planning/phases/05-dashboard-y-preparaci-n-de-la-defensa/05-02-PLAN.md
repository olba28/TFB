---
phase: 05-dashboard-y-preparaci-n-de-la-defensa
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/dashboard/plots.py
  - tests/dashboard/test_plots.py
autonomous: true
requirements: [DASH-04]
must_haves:
  truths:
    - "A pure builder returns an animated Plotly choropleth over all 23 years (2000–2022) with a fixed color range, so the map does not flicker between animation frames (DASH-04)."
    - "The scenario plot renders the -10/-20/-30% counterfactual central estimates with their bootstrap CIs; the PDP builder wraps interpret.partial_dependence_plots for embedding."
    - "Every builder is a pure function (DataFrame/array in, Plotly/Matplotlib figure out) with no st.* calls, unit-testable without launching Streamlit."
  artifacts:
    - src/dashboard/plots.py
    - tests/dashboard/test_plots.py
  key_links:
    - "build_choropleth uses locationmode='ISO-3' against panel_clean.country_code (already ISO3) and an explicit range_color — consumed by the Mapa tab in 05-04."
    - "build_scenario_plot consumes the dict returned by simulate.bootstrap_counterfactual (keyed by pct, with ci_2.5/ci_97.5)."
---

<objective>
Build the pure Plotly/Matplotlib figure builders (`src/dashboard/plots.py`) and their unit tests. These functions take DataFrames/result dicts and return figures with no Streamlit dependency, so they are fully testable under plain pytest and reusable across tabs.

Purpose: Isolate all figure construction (the animated choropleth with fixed color range, the multi-scenario CI plot, the partial-dependence embed) from Streamlit rendering, mirroring the project's existing pure-vs-IO separation (`panel_base.py` pure / `db.py` IO). This is where DASH-04's animation contract is implemented and verified.
Output: `plots.py` with `build_choropleth`, `build_scenario_plot`, `build_pdp`; unit tests proving the animation covers all 23 years and pins the color range.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dashboard-y-preparaci-n-de-la-defensa/05-CONTEXT.md
@.planning/phases/05-dashboard-y-preparaci-n-de-la-defensa/05-RESEARCH.md
@.planning/phases/05-dashboard-y-preparaci-n-de-la-defensa/05-PATTERNS.md
@.planning/phases/05-dashboard-y-preparaci-n-de-la-defensa/05-UI-SPEC.md
@src/panel_base.py
@src/simulate.py
@src/interpret.py
</context>

## Artifacts this phase produces (this plan)

- `src/dashboard/plots.py`:
  - `build_choropleth(df: pd.DataFrame, indicator_col: str, title: str) -> go.Figure` — animated (`animation_frame="year"`) choropleth, `locationmode="ISO-3"`, fixed `range_color`, `color_continuous_scale="Viridis"`, Plotly margins per UI-SPEC spacing scale.
  - `build_scenario_plot(results: dict, title: str) -> go.Figure` — multi-scenario central estimate + bootstrap CI plot from `simulate.bootstrap_counterfactual` output.
  - `build_pdp(rf, X, features, ax=None)` — thin wrapper over `interpret.partial_dependence_plots` returning the display for embedding.
- `tests/dashboard/test_plots.py` — unit tests for the builders (DASH-04 coverage).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: build_choropleth — animated map with fixed color range (DASH-04)</name>
  <read_first>
    - .planning/phases/05-dashboard-y-preparaci-n-de-la-defensa/05-RESEARCH.md (Pattern 3, Code Examples: choropleth animado; Pitfall 4)
    - .planning/phases/05-dashboard-y-preparaci-n-de-la-defensa/05-PATTERNS.md (plots.py section — verbatim Plotly code)
    - .planning/phases/05-dashboard-y-preparaci-n-de-la-defensa/05-UI-SPEC.md (Color: Viridis + range_color; Spacing: Plotly figure margins)
    - src/panel_base.py (pure-function, dep-var-agnostic signature style to mirror)
  </read_first>
  <behavior>
    - Test 1: build_choropleth(tiny_panel_df, "6.4.2", title) returns a Plotly Figure whose animation frames cover exactly the 23 years 2000..2022 (one frame per year present in df).
    - Test 2: the figure's coloraxis cmin/cmax equal the global min/max of the indicator column across all years (fixed range_color, not per-frame autoscale) — Pitfall 4.
    - Test 3: locationmode is "ISO-3" and locations bind to the country_code column.
  </behavior>
  <action>
    Add `build_choropleth(df, indicator_col, title)` to `src/dashboard/plots.py` using `plotly.express.choropleth` with `locations="country_code"`, `locationmode="ISO-3"` (panel_clean.country_code is already ISO3 — no transform), `color=indicator_col`, `animation_frame="year"`, `color_continuous_scale="Viridis"`, and `range_color=(df[indicator_col].min(), df[indicator_col].max())` computed over the FULL 2000–2022 range so the scale is stable across frames (RESEARCH Pitfall 4). Apply Plotly figure margins per the UI-SPEC spacing scale (l/r = 4px, b = 8px, t = 32px when the figure has its own title) via `update_layout(margin=...)`. The function must be pure: no `st.*` import, no I/O — DataFrame in, `plotly.graph_objects.Figure` out. Google-style docstring citing DASH-04, D-05 (native Plotly animation), and Pitfall 4 (why range_color is fixed). Follow panel_base.py import ordering (`from __future__ import annotations` first, then third-party, alphabetical-ish).
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/dashboard/test_plots.py -x -q 2>&1 | tail -8</automated>
  </verify>
  <done>build_choropleth returns an animated Figure with 23 year-frames and a coloraxis range fixed to the indicator's global min/max; no Streamlit import in plots.py.</done>
  <acceptance_criteria>
    - `plots.build_choropleth` returns a `plotly.graph_objects.Figure`.
    - The figure has one animation frame per year 2000..2022 (23 frames) when passed the `tiny_panel_df` fixture.
    - `fig.layout.coloraxis.cmin`/`cmax` equal the indicator column's global min/max.
    - `plots.py` contains no `import streamlit`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: build_scenario_plot + build_pdp (Simulación & SHAP figures)</name>
  <read_first>
    - src/simulate.py (bootstrap_counterfactual return dict shape: keyed by pct, each with effect_draws/ci_2.5/ci_97.5/excluded_countries)
    - src/interpret.py (partial_dependence_plots signature)
    - .planning/phases/05-dashboard-y-preparaci-n-de-la-defensa/05-UI-SPEC.md (Color: accent #1B6CA8 for central estimate; SHAP palette exception)
  </read_first>
  <behavior>
    - Test 1: build_scenario_plot on a synthetic results dict with keys -0.10/-0.20/-0.30 returns a Figure with one plotted marker/point per scenario and error bars derived from ci_2.5/ci_97.5.
    - Test 2: build_scenario_plot uses the accent color #1B6CA8 for the central-estimate series (UI-SPEC accent).
    - Test 3: build_pdp returns the object produced by interpret.partial_dependence_plots (delegation, no reimplementation).
  </behavior>
  <action>
    Add `build_scenario_plot(results, title)` to `plots.py`: it consumes the dict returned by `simulate.bootstrap_counterfactual` (keys are the signed reduction pcts, e.g. -0.10/-0.20/-0.30; each value has `ci_2.5`, `ci_97.5`, `effect_draws`). Plot the central estimate per scenario (mean of `effect_draws` across replicas, aggregated across surviving countries — document the aggregation choice in the docstring) with asymmetric error bars from the 2.5/97.5 percentile CIs, using the UI-SPEC accent `#1B6CA8` for the central-estimate marker. Return a `go.Figure` with UI-SPEC margins. Frame it explicitly (title/axis labels in Spanish) as a sensitivity analysis, not a per-country prediction (matches Phase 4 INTERP-03 framing).
    Add `build_pdp(rf, X, features, ax=None)` as a thin wrapper that calls `interpret.partial_dependence_plots(rf, X, features, ax=ax)` and returns its display — do NOT reimplement PDP math (INTERP-05 already owns it). Both functions pure (no `st.*`). Do not restyle SHAP colors — the SHAP summary uses shap's own palette and is rendered directly in the app tab (UI-SPEC SHAP exception), so plots.py provides no SHAP-recoloring builder.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/dashboard/test_plots.py -x -q 2>&1 | tail -8</automated>
  </verify>
  <done>build_scenario_plot renders scenario central estimates with CI error bars in the accent color; build_pdp delegates to interpret.partial_dependence_plots.</done>
  <acceptance_criteria>
    - `plots.build_scenario_plot` returns a `go.Figure` with one scenario entry per key in the results dict and error bars sourced from ci_2.5/ci_97.5.
    - The central-estimate marker color is `#1B6CA8`.
    - `plots.build_pdp` returns exactly what `interpret.partial_dependence_plots` returns (verified by patching/spying that it is called with the same args).
    - No `import streamlit` in plots.py.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Unit tests for plots.py (DASH-04)</name>
  <read_first>
    - .planning/phases/05-dashboard-y-preparaci-n-de-la-defensa/05-VALIDATION.md (Per-Task Verification Map: DASH-04)
    - tests/dashboard/conftest.py (tiny_panel_df fixture — created in 05-01)
    - tests/test_interpret.py (house style for testing pure functions with synthetic DataFrames)
  </read_first>
  <action>
    Create `tests/dashboard/test_plots.py` covering the behaviors above. Include the named test `test_choropleth_has_all_years` (asserts 23 animation frames for years 2000–2022 and the fixed `range_color` = global min/max) exactly as VALIDATION.md's DASH-04 row specifies. Add `test_choropleth_uses_iso3_locationmode`, `test_scenario_plot_has_ci_error_bars`, `test_scenario_plot_uses_accent_color`, and `test_build_pdp_delegates` (monkeypatch `interpret.partial_dependence_plots` to confirm delegation). Add a static guard test `test_plots_module_has_no_streamlit_import` asserting the string sequence for a Streamlit import does not appear in the `src/dashboard/plots.py` source text (read the file and check) — plots must stay pure. Module docstring cites DASH-04 and names the source module under test, per tests/test_simulate.py convention. Use the `tiny_panel_df` fixture, never real production data.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/dashboard/test_plots.py -q 2>&1 | tail -8</automated>
  </verify>
  <done>All test_plots.py tests pass, including test_choropleth_has_all_years and the no-Streamlit-import guard.</done>
  <acceptance_criteria>
    - `pytest tests/dashboard/test_plots.py` passes.
    - `test_choropleth_has_all_years` exists and asserts both the 23-year frame count and the fixed color range.
    - A test asserts `src/dashboard/plots.py` source contains no Streamlit import.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data (DataFrame/dict)→figure builder | Builders receive already-loaded, project-produced DataFrames/result dicts; they perform no I/O and no network access. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-5-01 | Tampering (input handling) | `build_choropleth` indicator_col argument | low | mitigate | `indicator_col` is used only as a pandas column selector, never interpolated into SQL or a shell/network call; the caller (05-04) restricts it to the fixed INDICATOR_LABELS/registry column whitelist. |
| T-5-DoS | Denial of Service | animated choropleth over 23 frames | low | accept | Frame count is bounded (23 years, ~215 countries) and rendered client-side by Plotly; acceptable for a single-user local demo. |
| T-5-SC | Supply Chain | package installs | low | accept | No new package installed — plotly is already pinned; no `kaleido` this phase. |
</threat_model>

<verification>
- `.venv/Scripts/python.exe -m pytest tests/dashboard/test_plots.py -q` green.
- `src/dashboard/plots.py` imports no Streamlit (guard test).
</verification>

<success_criteria>
- Pure, Streamlit-free figure builders for choropleth (animated, fixed color range — DASH-04), scenario CI plot, and PDP embed.
- All builder behaviors covered by fast fixture-based unit tests.
</success_criteria>

<output>
Create `.planning/phases/05-dashboard-y-preparaci-n-de-la-defensa/05-02-SUMMARY.md` when done.
</output>
